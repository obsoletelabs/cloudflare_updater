services:
  app:
    build:
      dockerfile: Dockerfile
    #image: my-python-app
    container_name: dev
    restart: unless-stopped

    env_file:
      - config/.env
    
    environment:
      #- WHOAMI_URLS=${WHOAMI_URLS}
      #- API_TOKEN=${API_TOKEN}
      - INTERVAL_SECONDS=10

    # Remove for production this is only for development!
    volumes:
      - ./src/cloudflare_updater:/app
      - ./config:/config