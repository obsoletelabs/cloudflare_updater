services:
  app:
    build:
      context: ./src
      dockerfile: Dockerfile
    image: my-python-app
    container_name: my-python-app
    restart: unless-stopped
    volumes:
      - ./src/app:/app
    command: python main.py
